[package]
name = "intl-codes"
version = "0.1.0"
edition = "2024"
readme = "README.org"
license = "MIT"
keywords = ["geographic", "country", "geo", "enum"]

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
strum = { version = "0.28.0", features = ["derive"] }
